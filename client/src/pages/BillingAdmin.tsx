import DashboardLayout from "@/components/DashboardLayout";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import { WorkspacePageHeader, WorkspaceSection, WorkspaceState } from "@/components/workspace/WorkspacePrimitives";
import { useAuth } from "@/_core/hooks/useAuth";
import { trpc } from "@/lib/trpc";
import { useEffect, useState } from "react";
import { toast } from "sonner";
import { useLocation } from "wouter";

export default function BillingAdmin() {
  const { user, loading } = useAuth(); const [, go] = useLocation(); const utils = trpc.useUtils(); const config = trpc.billing.adminConfig.useQuery(undefined, { enabled: user?.role === "admin" });
  const [form, setForm] = useState({ beneficiaryName: "", personalNumber: "", accountNumber: "", monthlyPriceGel: "", transferCommentPrefix: "SF", privacyNoticeKa: "ქვითარი ხელით მოწმდება SalonFlow platform admin-ის მიერ." }); const [formError, setFormError] = useState(""); const [search, setSearch] = useState(""); const list = trpc.billing.adminList.useQuery({ search: search || undefined }, { enabled: user?.role === "admin" });
  const save = trpc.billing.saveConfig.useMutation({ onSuccess: async () => { await utils.billing.adminConfig.invalidate(); toast.success("რეკვიზიტები შენახულია."); }, onError: e => toast.error(e.message) }); const approve = trpc.billing.approveMonthly.useMutation({ onSuccess: async result => { await utils.billing.adminList.invalidate(); toast.success(`1-თვიანი წვდომა გააქტიურდა ${new Date(result.endsAt).toLocaleDateString("ka-GE")}-მდე.`); }, onError: e => toast.error(e.message) }); const reject = trpc.billing.rejectReceipt.useMutation({ onSuccess: async () => { await utils.billing.adminList.invalidate(); toast.success("ქვითარი უარყოფილია; მფლობელს შეუძლია ხელახლა გაგზავნა."); }, onError: e => toast.error(e.message) }); const bonus = trpc.billing.grantBonusDays.useMutation({ onSuccess: async result => { await utils.billing.adminList.invalidate(); toast.success(`ბონუს დღეები დაემატა ${new Date(result.endsAt).toLocaleDateString("ka-GE")}-მდე.`); }, onError: e => toast.error(e.message) });
  useEffect(() => { if (!loading && user && user.role !== "admin") go("/app/today"); }, [go, loading, user]);
  if (!loading && user && user.role !== "admin") return <DashboardLayout><WorkspaceState kind="loading" title="თქვენს სამუშაო სივრცეში გადამისამართება…" /></DashboardLayout>;
  if (loading || !user) return <DashboardLayout><WorkspaceState kind="loading" title="წვდომა მოწმდება…" /></DashboardLayout>;
  const c = config.data; const value = (key: Exclude<keyof typeof form, "monthlyPriceGel">) => form[key] || String(c?.[key as keyof NonNullable<typeof c>] ?? ""); const gelValue = form.monthlyPriceGel || (c?.monthlyPriceTetri ? (c.monthlyPriceTetri / 100).toFixed(2) : "");
  const submitConfig = () => { const priceGel = Number(gelValue.replace(",", ".")); if (!value("beneficiaryName") || !value("personalNumber") || !value("accountNumber") || !Number.isFinite(priceGel) || priceGel <= 0) { setFormError("შეავსეთ ბენეფიციარი, პირადი ნომერი, ანგარიში და სწორი თვიური ფასი ლარში."); return; } setFormError(""); save.mutate({ beneficiaryName: value("beneficiaryName"), personalNumber: value("personalNumber"), accountNumber: value("accountNumber"), monthlyPriceTetri: Math.round(priceGel * 100), transferCommentPrefix: value("transferCommentPrefix"), privacyNoticeKa: value("privacyNoticeKa") }); };
  return <DashboardLayout><main className="sf-workspace-page mx-auto w-full max-w-6xl space-y-5"><WorkspacePageHeader eyebrow="PLATFORM ADMIN" title="Billing payments" description="ხელით საბანკო გადარიცხვის რეკვიზიტები და ქვითრების შემოწმება." /><WorkspaceSection title="გადახდის რეკვიზიტები" description="ეს მონაცემები გამოჩნდება მხოლოდ ვადაგასული workspace-ის მფლობელთან. სალონის მფლობელი ხედავს მხოლოდ აქ შენახულ ნამდვილ მონაცემებს."><div className="grid gap-4 md:grid-cols-2"><label className="grid gap-1.5 text-sm font-medium"><span>ბენეფიციარის სახელი</span><Input value={value("beneficiaryName")} onChange={e => setForm(x => ({ ...x, beneficiaryName: e.target.value }))} placeholder="მაგ. თამარ ტარაშვილი" autoComplete="name" /></label><label className="grid gap-1.5 text-sm font-medium"><span>პირადი ნომერი</span><Input value={value("personalNumber")} onChange={e => setForm(x => ({ ...x, personalNumber: e.target.value }))} placeholder="11 ციფრი" inputMode="numeric" autoComplete="off" /></label><label className="grid gap-1.5 text-sm font-medium"><span>ანგარიშის ნომერი</span><Input value={value("accountNumber")} onChange={e => setForm(x => ({ ...x, accountNumber: e.target.value }))} placeholder="GE00TB0000000000000000" autoCapitalize="characters" autoComplete="off" /></label><label className="grid gap-1.5 text-sm font-medium"><span>1-თვიანი პაკეტის ფასი (₾)</span><Input value={gelValue} onChange={e => setForm(x => ({ ...x, monthlyPriceGel: e.target.value }))} placeholder="მაგ. 49.00" inputMode="decimal" /></label><label className="grid gap-1.5 text-sm font-medium"><span>გადარიცხვის კომენტარის prefix</span><Input value={value("transferCommentPrefix")} onChange={e => setForm(x => ({ ...x, transferCommentPrefix: e.target.value.toUpperCase() }))} placeholder="SF" /></label><label className="grid gap-1.5 text-sm font-medium"><span>მფლობელისთვის ნაჩვენები კონფიდენციალურობის ტექსტი</span><Textarea value={value("privacyNoticeKa")} onChange={e => setForm(x => ({ ...x, privacyNoticeKa: e.target.value }))} className="min-h-20" /></label></div>{formError ? <p className="mt-4 text-sm text-destructive" role="alert">{formError}</p> : null}<div className="mt-5 flex flex-wrap items-center gap-3"><Button onClick={submitConfig} disabled={save.isPending}>{save.isPending ? "ინახება…" : "რეკვიზიტების შენახვა"}</Button><p className="text-xs leading-5 text-muted-foreground">ფასი შეიყვანეთ ლარში; სისტემა მას უსაფრთხოდ ინახავს თეთრებში.</p></div></WorkspaceSection><WorkspaceSection title="ქვითრები" description="დაადასტურეთ მხოლოდ ხელით შემოწმებული ქვითარი."><Input value={search} onChange={e => setSearch(e.target.value)} placeholder="სალონის სახელი, ID ან მფლობელის ელფოსტა" /><div className="mt-4 grid gap-3">{list.data?.map(item => <article key={item.id} className="rounded-2xl border p-4"><p className="font-semibold">{item.organizationName} · {item.billingCodeSnapshot}</p><p className="mt-1 text-sm text-muted-foreground">{item.ownerEmail} · {item.status} · {item.transferComment}</p><div className="mt-3 flex gap-2"><Button asChild size="sm" variant="outline"><a href={item.receiptUrl} target="_blank" rel="noreferrer">ქვითრის ნახვა</a></Button>{['SUBMITTED','UNDER_REVIEW'].includes(item.status) ? <><Button size="sm" onClick={() => approve.mutate({ submissionId: item.id })}>1 თვე</Button><Button size="sm" variant="outline" onClick={() => { const note = window.prompt("უარყოფის მოკლე მიზეზი"); if (note) reject.mutate({ submissionId: item.id, note }); }}>უარყოფა</Button><Button size="sm" variant="ghost" onClick={() => { const days = Number(window.prompt("ბონუს დღეების რაოდენობა")); const reason = window.prompt("ბონუსის მიზეზი"); if (Number.isInteger(days) && days > 0 && reason) bonus.mutate({ organizationId: item.organizationId, days, reason }); }}>ბონუს დღეები</Button></> : null}</div></article>)}{!list.isLoading && !list.data?.length ? <WorkspaceState kind="empty" title="ქვითარი არ არის" /> : null}</div></WorkspaceSection></main></DashboardLayout>;
}
