# Dark Luxury Visual QA Notes

## Foundation spot-check

| Route | Viewport | Confirmed rendered evidence |
|---|---:|---|
| `/` | 1440 × 900 | Default Dark Luxury direction renders with a deep obsidian canvas, terracotta conversion CTAs, jade status accent, Georgian-first oversized editorial heading, dark product preview, and surface-layered feature storytelling. |
| `/login` | 1440 × 900 | Local email/password login renders in the same Dark Luxury system with a deep split layout, warm text hierarchy, clearly visible violet keyboard focus, layered form card, and a terracotta primary action. |

The next visual pass must validate populated booking and authenticated operational data at mobile/tablet/desktop breakpoints. This document records only direct rendered checks; it does not claim unobserved accessibility or performance outcomes.

## Populated operational evidence

| Route | Viewport | Confirmed rendered evidence |
|---|---:|---|
| `/app/today` | 1440 × 1000 | The disposable workspace shows live seeded queue rows, Georgian status/payment labels, integer-tetri GEL metrics, remaining balance, compact KPI cards, active branch selector, and role-permitted action controls in the dark operations shell. |
| `/app/calendar` | 375 × 812 | The mobile route preserves the day/list treatment rather than shrinking the desktop resource grid. Branch selection, day/week controls, specialist selection, availability count and the operational empty/agenda area remain readable on dark layered surfaces. |

All screenshot data comes from a temporary local owner/workspace and is deleted in foreign-key-safe order after the capture command completes.
